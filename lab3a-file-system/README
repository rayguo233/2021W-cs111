Files
    ext2_fs.h   - helper header file with structs
    lab3.c      - source code
    Makefile    - Build and run the deliverable program. 
                  The higher level targets are:
                    default - compile your program (with the 
                              -Wall and -Wextra options) to produce 
                              an executable named lab3a
                    dist    - create the deliverable tarball
                    clean   - delete all programs and output 
                              generated by the Makefile.
    README      - text file containing descriptions of each of the 
                  included files and any other comments

Comments
    In the code I assumed there will only be one group.